apiVersion: v1
kind: Service
metadata:
  name: fraud-detector-svc
spec:
  type: NodePort
  selector:
    app: fraud-detector
  ports:
    - port: 8080
      # targetPort bug -- nginx:alpine listens on 80, not 8080.
      targetPort: 80
      nodePort: 30092
